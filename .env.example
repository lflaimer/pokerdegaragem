# Database
DATABASE_URL="mysql://poker:poker123@localhost:3306/poker_db"

# Auth - Generate a secure random string for production
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Admin Panel
# Username for admin access
ADMIN_USERNAME="admin"
# Password hash (bcrypt) - Generate with: node -e "require('bcryptjs').hash('YourPassword', 12).then(console.log)"
ADMIN_PASSWORD_HASH="your-bcrypt-hash-here"
# Separate JWT secret for admin tokens
ADMIN_JWT_SECRET="admin-jwt-secret-change-in-production"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
